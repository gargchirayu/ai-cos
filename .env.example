# Required only to (re)run the analysis pipeline: `python analyze.py`.
# The dashboard itself never needs a key — it serves the committed data/analysis.json.
ANTHROPIC_API_KEY=

# Optional: override the model used by the analysis pipeline (default: claude-opus-4-8)
# COS_MODEL=claude-sonnet-4-6
